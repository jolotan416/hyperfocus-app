package com.spotlight.spotlightapp.task

import com.spotlight.spotlightapp.R
import com.spotlight.spotlightapp.data.ErrorEntity

object TaskErrorEntity {
    val MAX_DAILY_INTENT_TASK_REACHED = ErrorEntity(
        R.string.maximum_daily_intent_task_error_message)
}